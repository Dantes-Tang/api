<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"  
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  
    xsi:schemaLocation="http://www.springframework.org/schema/beans  
           http://www.springframework.org/schema/beans/spring-beans-2.5.xsd          
           ">  
  

  
    <!-- 邮件的发送器，非常的重要 ，传入配置文件的信息-->
	<bean name="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">

		<property name="host" value="MTACOR7.capgemini.com" />
		<property name="username" value="gorgao" />
		<property name="password" value="Qq123456" />
		<property name="javaMailProperties">
			<props>
				<prop key="mail.smtp.auth">true</prop>
				<prop key="mail.debug">true</prop>
				<prop key="mail.smtp.timeout">25000</prop>
				<prop key="mail.smtp.sendpartial">true</prop>
			</props>
		</property>
	</bean>  
  

  
    <!-- 读取velocity模板的工厂类，也就是的不同的模板的 -->  
    <bean id="velocityEngine"  
        class="org.springframework.ui.velocity.VelocityEngineFactoryBean">  
        <property name="velocityProperties">  
            <props>  
                <!-- 设置从classPath下读取模板文件 vm文件 -->  
                <prop key="resource.loader">class</prop>  
                <prop key="class.resource.loader.class">  
                    org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader  
                </prop>  
                <prop key="velocimacro.library"></prop>  
            </props>  
        </property>  
    </bean>  
    
    
       <!--mail 发送工具 -->
       <bean id="VelocityMailUtil" class="com.capgemini.drms.util.VelocityMailUtil">
              <property name="mailSender" ref="mailSender" />
              <property name="velocityEngine" ref="velocityEngine" />
              <property name="templateName" value="../../emailTemplate.html" />
              <property name="from" value="hzdrmssystem.fssbu@capgemini.com" />
              <property name="subject" value="DRMS DEMAND REQUEST-" />
       </bean>

</beans>